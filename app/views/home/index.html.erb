<div class="perfil">
	<% if user_signed_in? %>
		<div class="row">
			<div class="col s9 m8">
				<h3 class="coloreds">Bienvenido, <%= current_user.name %></h3>
			</div>

			<div class="col s3 m3">
				<div class="card-panel z-depth-2">
					<p class="big-padding"><%= current_user.name %></p>

					<i class="medium material-icons icon-perfil">account_box</i>

				</div>
			</div>
		</div>
	<% end %>
</div>
